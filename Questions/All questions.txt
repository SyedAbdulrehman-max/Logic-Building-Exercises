Q1. Greet the User: Ask the user for their name and print a greeting like "Hello, Ali!".  
Q2. Add Two Numbers: Ask the user for two numbers and print their sum.  
Q3. Temperature Conversion: Ask the user for a temperature in Celsius and convert it to Fahrenheit.  
Q4. Even or Odd: Ask the user for a number and print whether it’s even or odd.  
Q5. Check Age for Voting: Ask the user for their age and print "You can vote" if 18 or older, otherwise "You cannot vote".  
Q6. Compare Two Numbers: Ask for two numbers and print which one is larger (or if they are equal).  
Q7. Multiplication Table: Ask the user for a number and print its multiplication table from 1 to 10.  
Q8. Sum of First N Numbers: Ask the user for a number n and print the sum of all numbers from 1 to n.  
Q9. Count Down: Ask the user for a number and count down from that number to 1.  
Q10. Check Word Length: Ask the user for a word and print "Short word" if it has less than 5 letters, otherwise "Long word".  
Q11. Count Vowels in a Word: Ask the user for a word and count how many vowels (a, e, i, o, u) it has.  
Q12. Simple Password Check: Ask the user to enter a password. If it matches "python123", print "Access granted", else "Access denied".  
Q13. List of Items: Create a list of 5 fruits and print the first and last fruit.  
Q14. Shopping Cart Total: Ask the user for the prices of 3 items and print the total cost.  
Q15. Reverse a String: Ask the user for a word and print it backwards.  
Q16. Find the Largest in a List: Create a list of numbers and print the largest one.  
Q17. Guess the Number Game: Set a number in your code (e.g., 7) and ask the user to guess it. If correct, print "You guessed it!", else "Try again".  
Q18. Sum of Even Numbers: Ask the user for a number n and print the sum of all even numbers from 1 to n.  
Q19. Word in a Sentence: Ask the user for a sentence and a word, and check if the word appears in the sentence.  
Q20. Simple Function Example: Write a function that takes a name as input and prints "Good morning, [name]!". Call it for at least two different names.  
Q21. Print numbers from 1 to 10 using a while loop.  
Q22. Print all even numbers between 1 and 20 using a while loop.  
Q23. Calculate the sum of numbers from 1 to n (user input).  
Q24. Print the following pattern using a while loop: *, **, ***, ****  
Q25. Take a number as input and print its digits in reverse.  
Q26. Find the factorial of a number using a while loop.  
Q27. Take numbers from the user until they enter 0, then print the sum.  
Q28. Print all numbers between 1 and 100 divisible by both 3 and 5.  
Q29. Create a number guessing game: random number 1-50, user guesses until correct, give hints "Too high"/"Too low".  
Q30. Find the smallest number divisible by all numbers from 1 to n (LCM problem).  
